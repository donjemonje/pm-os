# QA log

Append-only, kept by Dana (`qa-manager`). Every feature branch adds its row
BEFORE review — no row, no review. Newest entries at the bottom.

| Feature branch | Date | Tests added/updated | QA'd by |
| --- | --- | --- | --- |
| feature/tests-setup | 2026-08-26 | Added tests/e2e/auth.spec.ts (login renders, RoomLens login + app shell, docs/releases nav) | Omri |
| feature/tests-setup | 2026-08-26 | Added tests/e2e/all-pages.spec.ts (all-pages sweep, logged-in redirects, CRM auth-realm isolation, ideas 404s) + expectAppPageRenders helper; removed auth.spec.ts docs/releases nav test (subsumed by the sweep) | Dana & Omri |
| feature/tests-setup | 2026-08-26 | Infra, no test changes: env loading moved into playwright.config.ts (bare npx/--ui runs match npm script); env guard in global-setup.ts + test-env.ts (login enabled, pmos_test only, SESSION_SECRET, port/app-URL match, IDEAS_ENABLED off). Guard failure path verified by removing yaml | Dana & Omri |
| feature/tests-setup | 2026-08-26 | Prod-testing removal (Daniel's order), no assertion changes: prod-smoke project + scripts + smoke tags stripped; seed renamed scripts/seed-test-db.mjs, env overrides removed, hardcoded synthetic creds, refuses NODE_ENV=production; env guard unchanged. Repo grep clean for prod terms | Dana & Omri |
| feature/npm-audit-fixes | 2026-08-26 | None added — dependency/security chore, no new product behavior (audit bumps; Next 15.5.23→16.3.2; middleware.ts→proxy.ts; lint script removed; jsx react-jsx). Existing suite already covers the risky surface: redirect/realm specs exercise the renamed proxy, all-pages sweep covers rendering. Full suite re-run green on Next 16 (6/6, 13.0s); clean build 48/48 pages. Accepted residual: deepmerge-ts <8 via @prisma/config pin (dev CLI only) | Dana |
| feature/admin-restriction | 2026-08-27 | Added tests/e2e/admin.spec.ts (A1 promote/demote + self-role/self-deactivate guardrail refusals in UI; A2 per-org ideas override gates a second user's context On→reachable / Default→404; A3 deactivate revokes live session + blocks login, reactivate restores in the same context — tightened after the stale-cookie 500/lockout bug Omri found, fixed in fec4188 via the /api/auth/session-expired exit ramp). Updated: all-pages.spec CRM auth-realm test replaced by USER-404-on-/admin (CRM became role-gated /admin — subsumption); helpers.ts adds QA_ADMIN + loginAsRoomLensAdmin; two-factor-helpers.ts per-secret code-window tracker (backward compatible); seed-test-db.mjs seeds qa+roomlens-admin (PMOS_ADMIN, own synthetic TOTP secret) and resets roles/deactivation/org overrides; admin.spec resets fixtures in beforeAll+afterAll. Not e2e: last-active-admin guard stays unit-level (actor is always the last admin, self-rule fires first — unreachable end-to-end). Dedup ruling: A2's baseline /ideas 404 kept alongside the all-pages ideas test (load-bearing precondition, not a duplicate flow). Suite green by Dana: 13 passed, 1 env-gated skip | Dana & Omri |
| feature/2fa | 2026-08-26 | Added tests/e2e/two-factor.spec.ts (T1 challenge layout, T2 previous-window rejected, T3+T4 valid code + no replay, T5 unverified session 401s, T6 forced enrollment) + two-factor-helpers.ts; added tests/e2e/register.spec.ts (signup→enrollment, env-gated skip while ALLOW_SIGNUP=false mirrors prod). Updated: loginAsRoomLens completes the mandatory TOTP challenge; seed-test-db.mjs enrolls qa+roomlens with the fixed test secret (app's own encryptTotpSecret, --experimental-strip-types), resets totp state + sessions each run; env guard checks TOTP_ENC_KEY (set + 64 hex); Playwright timeout 30s→60s (TOTP window waits by design). Removed: tests/e2e/seed-2fa.ts (targeted the DEV database — rule violation); auth.spec.ts app-shell login test (subsumed by T3 + all-pages sweep). Suite green twice by Dana (10 passed, 1 env-gated skip; second run with no re-seed) | Dana & Omri |
| feature/admin-restriction (rework) | 2026-08-27 | Second sign-off after post-review changes. Updated admin.spec A1 (rewritten for script-only roles, Daniel's security call: role PATCH 400s with a script pointer, bundled role+deactivate payload rejected whole with no partial application, UI renders zero role affordances — guarded against vacuous passes by asserting the Deactivate button IS in the same actions column; self-deactivation refusal kept, message asserted verbatim). Updated admin.spec A2 (moved to /admin/enablements after the Configurations rename; row-scoped selectors per flag; ideas full override round-trip kept; extended with a chat Off→404→Default round-trip covering the reversed env polarity — page and /api/chat/sessions both 404 — and restoring default before all-pages runs). Updated all-pages.spec (/admin/configurations → /admin/enablements in the USER-404 loop) and env guard (DOCS_ENABLED/CHAT_ENABLED must not resolve false-like — both on when unset). Dedup ruling: no separate docs toggle test — docs rides the identical resolver/gate path (featureEnabledForCurrentUser + orgFeatureDisabledResponse + registry-driven Enablements UI, verified by grep across the 7 docs handlers and layout), override-wins is proven by ideas, polarity by chat, docs default-on rendering by the all-pages sweep. Accepted product gap (documented in 63647be): Releases attach-document UI still renders with docs off; its API 404s. Suite green by Dana: 13 passed, 1 env-gated skip, 3.5m | Dana & Omri |
| feature/customer-reporters | 2026-08-31 | Added tests/e2e/customer-reporters.spec.ts (CR1 Settings → Ideas Customers panel add / case-insensitive duplicate rejection / delete, with reload-persistence checks — first coverage of the shared KINDS CRUD panel; CR2 drawer derives reporters from ticket requesters and customers from affectedCustomers, cataloged name renders teal, off-catalog name renders as amber suggested chip, ✓ approve adds it to the catalog + flips the chip teal live + logs approve_customer; CR3 ✕ dismiss removes the chip, "dismissed" toggle shows the strikethrough entry, restore brings the suggestion back untouched-by-catalog, dismiss_customer + undismiss_customer both on the ledger; CR4 list rows carry chips with the ⚑ suggested flag, Customer FilterRow narrows to matching ideas and clears via the selected chip). Fixtures seeded via Prisma in beforeAll exactly as the v5 extract stage would persist them (no LLM calls), QA-CR-prefixed, delete-and-recreate + afterAll cleanup so reruns need no re-seed. Ruling: ideas stays OFF in env + seed; the spec sets the per-org {ideas: true} override in beforeAll and clears it in afterAll (admin A2 pattern) — enabling it in the seed would break the all-pages ideas-404 sweep. Residual narrower than feared: admin.spec runs first alphabetically and resets features:{} in beforeAll, so a crash-leaked override self-heals on any full-suite rerun; only a filtered rerun skipping admin.spec can hit it. Dedup: none — no existing test renders /ideas or /settings/ideas content; the override-set is a fixture precondition, not a duplicate of admin A2's override flow. Suite green by Dana: 17 passed, 1 env-gated skip, 5.7m | Dana & Omri |
| feature/ideas-jira-merge | 2026-08-31 | Added tests/e2e/ideas-jira-merge.spec.ts (JM1 Final page hides unchanged ideas by default, Unchanged chip reveals them, Import Status chips are single-select — click replaces, re-click clears; JM2 approval exemption: unchanged rows never grow the Mark-reviewed affordance, drawer has no Approve button, and /api/ideas/mutate refuses decision "reviewed" for an approval-exempt idea — no-op verified in returned state AND DB, with a positive control flipping the New idea on the same endpoint then restoring it; JM3 Merge view: unchanged ideas + Jira sources backing only unchanged ideas hide under Merge/Single and show under Unchanged, edit mode shows the full source pool; JM4 manual reassign: attaching a ticket flips an unchanged Jira idea to Updated with the zendesk-vote delta (+1 badge, newVotes 0→1, existingVotes untouched), detaching rolls status and delta back). LLM-free: the Vertex match stage stays an accepted e2e gap; fixtures (JiraIdeaSnapshot + Jira-origin ideas by jiraKey, ZendeskTicketRaw + zendesk IdeaSources, QA-JM prefixes) are Prisma-seeded exactly as the import would persist them, delete-and-recreate beforeAll + afterAll cleanup, JM2/JM4 reverse their own writes. Ideas gating: per-org {ideas: true} override set in beforeAll / cleared in afterAll (customer-reporters pattern; env + seed stay ideas-off for the all-pages 404 sweep — spec order and admin.spec's reset keep the residual self-healing). Dedup: none — no prior test touches Import Status chips, the merge board, approval exemption, or reassign; the override-set is a fixture precondition, not a re-test of admin A2. Updated customer-reporters.spec CR1 (found in Dana's verification run): first-hit customers-API POST can outlive the default 5s expect timeout under local `next dev` on-demand compile — snapshot showed the form correctly disabled mid-flight, product fine; the post-Add visibility assert now has a 15s timeout (timing-by-design headroom, same class as the TOTP waits; no assertion loosened). Suite green by Dana after the fix: 21 passed, 1 env-gated skip, 7.6m (first run had the CR1 cold-compile failure, reported as found) | Dana & Omri |
| feature/google-sso | 2026-09-01 | Added tests/e2e/google-sso.spec.ts (G1 googleSso system-flag round-trip on the new /admin/enablements System card — Off (default)→On→Off→Default, each state observed on /api/auth/oauth/providers AND the authorize redirect: Google consent URL when on, /login?error=google_sso_disabled when off; fake test creds make the provider "configured", no call ever reaches Google; G2 forgot-password enumeration safety — identical 200 {ok:true} for real and nonexistent email, token row exists only for the real user, unused, future expiry; G3 full reset path — DB-inserted token hashed exactly as requestPasswordReset stores it → /reset-password UI sets the new password → same-token replay 400s without clobbering it → new password logs in through the mandatory TOTP challenge; G4 expired token gets the same generic 400 and the row stays unused). State discipline: beforeAll+afterAll clear the googleSso override, purge PasswordResetToken, and restore QA_USER's seeded passwordHash via a fresh app-format re-hash — no leak into other specs, rerunnable without reseed (verified). Updated global-setup.ts env guard (DISABLE_GOOGLE_LOGIN must resolve true-like; fake GOOGLE_CLIENT_ID/SECRET required) with matching creds in test-apphosting(.example).yaml + CI. Updated auth.spec (Dana): the logged-out render test now clicks the /login "Forgot password?" link and asserts the /forgot-password form — the link is the reset flow's only entry point and G2–G4 bypass it via API/direct URL, so it could vanish silently otherwise; also proves the proxy public-path entry. Updated all-pages.spec route inventory (comment only): /forgot-password + /reset-password documented as public, covered in auth.spec/G3, deliberately absent from the logged-in redirect loop (proxy does not bounce a signed-in user off them — same class as /login/2fa). Dedup: none — G1 is first coverage of the SystemFlag mechanism, distinct from admin A2's per-org override per the 2026-08-27 registry ruling; G3's TOTP login is a means of proving the password change, not a re-test of two-factor T3. Accepted gap, flagged in the spec header: sticky login type (email_uses_password in signInWithOAuth) needs a mocked Google token endpoint — review-covered only. Suite green by Dana: 25 passed, 1 env-gated skip, 7.9m; auth+google-sso rerun green without reseed | Dana & Omri |
| feature/ci-test-fixes | 2026-09-02 | None added — suite-repair chore for the two CI failures on development since the feature/support_import_to_jira merge (85be916), both test-only defects, product behaviour correct. Updated admin.spec A2: the merge added an "Ideas undo" row to /admin/enablements, so the row-scoped `li` locator with hasText "Ideas" matched two rows (strict-mode violation); now `.filter({ hasNotText: "Ideas undo" })` — verified against the FLAGS registry that no other label/description contains "ideas" (hasText is case-insensitive substring; a regex `/^Ideas\s+(On|Off)\b/` was tried first and fails because the post-click row text is "Ideas OnIdeas pipeline…" with no word boundary — do not retry). Updated ideas-jira-push.spec PJ2: the invalid-JSON case sent a string body under a JSON content-type, which Playwright 1.62 JSON.stringify-es when unparsable, so the route received a valid JSON string and the assertion failed on "mode must be preview or execute"; now `data: Buffer.from("{not json")` reaches the route's JSON catch untouched — PJ2's "Invalid JSON" assertion is exercised for real for the first time. No assertion loosened in either fix. Process finding for the record: feature/support_import_to_jira merged with no QA-LOG row and with a red suite (ideas-jira-push.spec PJ1–PJ3 entered the suite via that merge without a sign-off row). Suite: full run green by engineering (28 passed, 1 env-gated skip, 8.9m); admin + ideas-jira-push re-run green by Dana (6 passed, 2.7m) | Dana |
| feature/admin-delete-user-org | 2026-09-03 | Added tests/e2e/admin-delete-user-org.spec.ts (AD1 row "Delete" via window.confirm removes the user in UI + DB, DELETE self → 400 "You cannot delete your own account", org DELETE without the confirm word → 400, DELETE of the actor's own org → 400 "You cannot delete your own organization", type-"delete" dialog: wrong word + Enter is inert, Esc cancels, "delete" + Enter removes the org, its workspace and its remaining user; AD2 invite by password: POST /api/admin/users → invite {sent:true, delivered:false} (no SMTP → console), /invite rejects a garbage token and a reset token of a password user, real invite shows "Complete your sign-up" with no Google button (DISABLE_GOOGLE_LOGIN pinned) → "Sign Up with Credentials" → live checklist li[data-rule][data-ok] + submit disabled until valid and matching → lands on /login/2fa enrollment → same token replay 400 + invite link dead → Admin row flips to "Password" with no Resend button; AD3 Access column "Invite pending"/"Password", Resend invite only on pending rows, POST …/invite → 200 {ok, delivered:false} leaving exactly one live token, → 400 for a password user, UI resend shows the no-SMTP notice; AD4 landing order: dashboard Off → "/" and /dashboard land on /docs, ideas On → /ideas, docs Off → /releases + /api/releases 404 and "/" → /chat, overrides cleared → /dashboard and Releases back). Fixtures created through the admin API (QA-ADO orgs, qa+ado-* users), reset in beforeAll+afterAll incl. RoomLens features {}; nothing depends on activeAdminCount (dev clones hold >1 admin). Added helpers.ts withTestDb (shared Prisma-on-RESOLVED_ENV helper; existing specs keep their local copies for now). Product finding, unasserted pending Dana: resetPassword (password path) does not revoke a user's other unused tokens — probe: sibling token → 200 on /api/auth/reset-password after sign-up; Google path revokes. Reachable only after a failed SMTP resend. Env note: feature QA on the clone needs the worktree yaml NEXT_PUBLIC_APP_URL=http://localhost:3201 (guard refuses otherwise). First full run on pmos_ft_admin_delete_user_org, port 3201: 30 passed, 2 failed (ideas-jira-push PJ2/PJ3 — clone state: RoomLens's cloned workspace had a live JiraConnection and the test env has no ATLASSIAN creds → "reading 'trim'" instead of "Jira not connected"). Fixed by engineering: seed-test-db.mjs now strips Jira/Drive connections from the QA workspace (feature QA runs on dev clones since 09-03) and getValidConnection returns not-connected when OAuth creds are absent; resetPassword now revokes sibling unused tokens (Omri's AD2 finding) and AD2 asserts it. Final full suite: 32 passed, 0 failed, 11.2m. New spec green twice (second run without reseed) | Omri |
| feature/ui_facelift_v1 | 2026-09-13 | Added tests/e2e/ui-facelift.spec.ts (UF1 Ideas page one-row toolbar, Status popover with All, "Review in Progress" card, Jira Merge disabled without a connection, no score text; UF2 Settings → Customers color swatch persists a palette pick, near-duplicate names show "Possible duplicate" → Merge into → alias listed → split back out; UF3 idea drawer edit adds a customer by Enter, removes a ticket-derived one (dismissal), toggles a product line, Save reflects all; UF4 Admin → Ideas Import runs card empty state + `{ runs: [] }` for a fresh org, a batch appears after Refresh, API 401/403 for non-admins). Updated customer-reporters.spec (Customers moved to /settings/ideas/customers, per-source "Reported by" lines, palette-colored chips instead of fixed teal) and ideas-jira-merge.spec for the renamed UI. Full suite green; six first attempts of login-dependent tests hung while the machine was offline (Google sign-in script) and passed on retry — harness, not product. | Omri (tests), Claude (run + log) |
| qa/release-2.0.0 (release QA 2.0.0 — baseline run) | 2026-09-14 | Config only, no test changes: playwright.config.ts `local` project gains an env-driven real-browser switch — `channel: process.env.PW_CHANNEL` + `headless: process.env.PW_HEADED ? false : undefined` (both unset = bundled headless Chromium, CI unaffected); .github/workflows/e2e.yml adds `--grep-invert @ai` (CI has no Vertex credentials). Baseline run of the existing suite on origin/development 22b1799 in worktree pmos-office/claude/pm-os/worktrees/release_qa_2_0_0, DB pmos_ft_release_qa (clone of pmos — the env guard accepts only pmos_test / pmos_ft_*), port 3299, real installed Google Chrome headed (launch log: one launch of /Applications/Google Chrome.app/…, no --headless). Result: 36 passed, 0 failed, 0 flaky, 13.4m — 36 not the handoff's 32: ui-facelift.spec UF1–UF4 landed 09-13 with its own row. Results, per-case mapping and the HTML report live in pmos-office office/qa/qa-data.js (run `2026-09-14-dev-22b1799`) and office/qa/runs/2026-09-14-dev-22b1799/. Process note: this session had no Agent tool, so Dana wrote steps C and D herself instead of handing them to Omri. | Dana |
| qa/release-2.0.0 (release QA 2.0.0 — new specs) | 2026-09-14 | Added tests/e2e/ai-golden.spec.ts (@ai, never in CI: ONE live Vertex-eu upload of tests/fixtures/roomlens-golden-v2.csv through the real "Upload Zendesk CSV" button, answer key roomlens-golden-v2.key.json generated together with it — 10 corpus tickets + QA ticket 031 (two separate asks, for the split stage) + one row with neither subject nor body; test.step per case IMP-01/02/03/04/05/08/10/11/13, REV-01, IMP-06; cost guard ≤ 60 AI calls; runs in its own org qa-golden with its own TOTP user, kept after the run for Daniel's read). Added auth-negative.spec.ts (AUTH-03 wrong/unknown → identical 401, empty → 400, never a cookie, form shows the message; AUTH-08 five wrong codes → 429, a correct code is refused while limited, challenge works after the 60 s window — timing by design, test.setTimeout 180 s; AUTH-25 /admin → /login?from=%2Fadmin, /api/admin/* 401). Added gating-isolation.spec.ts (GATE-02 prod-parity via per-org overrides: chat/docs/docs-new/releases pages + APIs 404, sidebar clean, /dashboard and / forward to /ideas — AD4's landing rule, not a 404 — Drive asserted gone too, soft sweep so one run lists every surface; GATE-06 second QA org with its own TOTP user sees only its rows on /api/ideas, lists and /ideas, RoomLens ids sent to mutate / lists / ideas-runs never change or return RoomLens rows — mutate answers 200 no-op today where the inventory said 404, both accepted, the invariant is no cross-org read or write; XC-02 anonymous sweep of 36 method+route pairs → 401/404 with an {error} body only; limit: /api/ideas/* 404 before auth while IDEAS_ENABLED is off, so auth removal there is not detectable in this env). Added admin-onboarding.spec.ts (ADM-02 the Kela path: Add user + "+ Create new organization…" → org, workspace, invite-pending user → admin enables Ideas for the org (off modules off) → invite link → Sign Up with Credentials → 2FA enrollment → lands on that org's empty Ideas; ADM-08 Kela-style CSV aliases saved via PUT ideas-config, remap fills module/customerName/whyBuild/insights/dealRelated/customerType/url from the raw rows, adds the truth customer, leaves catalogKind/catalogReason alone, drops "not a url" — own org, DB + API assertions (the drawer half is v2 territory); ADM-09 four seeded batches render Completed / Failed / Running / Stale — no heartbeat, the completed row expands into the stage table, a run that finishes in the DB flips without Refresh — UF4 keeps the empty state, Refresh and the USER-404 rule). Added ideas-rules.spec.ts (SET-01 admin add/rename/delete + case-insensitive 409, USER read + chip color only and 403 on every other write, settings page follows; IMP-07 empty file / no subject-description columns / prose renamed .csv / subject-only view export → UI error, no batch row, no ledger row; MRG-04 reassign gives 3 for jira-origin 2+2, 4 for zendesk-origin 2+2, consolidated untouched, and the verbatim prod recompute SQL agrees on the same rows — run inside a rolled-back transaction). Updated helpers.ts (loginWithTotp, hashPasswordLikeApp, seedQaOrgWithUser — the own-org recipe from face_lift_v2 QA) and two-factor-helpers.ts (passTwoFactorChallenge takes an optional `landed` locator; RoomLens callers unchanged). Dedup rulings: ADM-09 vs UF4 split as above; GATE-06 probes lists with a customer id because product-line writes are admin-only (SET-01 owns that); the golden spec needs no Clear import because its org is empty by construction. Runs (headed Chrome, same env, no reseed between them): new specs run 1 8/12 — four defects in the new tests (card-rooted `has:` and the nested stage table in ADM-09, a USER's 403 on product-line writes in GATE-06, /dashboard forwarding in GATE-02, wrong methods/params in XC-02), run 2 10/12, run 3 11 passed / 1 failed, 4.2m. The remaining red, GATE-02, is a product finding: /settings/google-drive renders Settings → Integrations with a live Connect link and /api/google-drive/status returns 200 while Google Drive is declared non-existent (Daniel 2026-09-13); every other surface in the sweep passed. Golden: one attempt, 57 s, no retry (threshold miss, not a provider error): import completed in 52.5 s, classify 10/10 vs the key, ledger 25 == 25 AI calls, 021+022 merged with 2 votes, 031 split, 029 over-segmented into two singles (acceptable per the key), 9 ideas, raw row byte-equal; IMP-10 FAILED on classify cached share 60% (< 70%) and IMP-11/13, REV-01, IMP-06 were not reached. After the run the golden threshold checks became expect.soft (thresholds unchanged) so the next attempt settles every case. No release sign-off — two findings stand (Drive gating, classify cache share); this row records what was QA'd. | Dana |
| feature/face_lift_v2 | 2026-09-14 | Added tests/e2e/ui-facelift-v2.spec.ts (FL1 sidebar rail starts collapsed with no pmos_sidebar cookie (w-16), "Expand menu" → w-60 + cookie "expanded" + green Ideas badge equal to the workspace's pending count, survives reload, "Collapse menu" → w-16 + cookie "collapsed"; the built-in "All Customers" row is bootstrapped by a /ideas visit in a workspace with zero customers, DELETE via /api/ideas/lists/customers → 400 "All Customers is built in and can't be deleted" and the row stays; FL2 "Clear import" opens the PM-OS confirm dialog — role=alertdialog, heading "Are you sure?", exactly Cancel + "Remove all" (last, focused) — Esc closes it with the row and DB untouched (1 idea / 1 ticket), reopen + Enter clears: "No ideas yet", Clear import button gone, 0 ideas / 0 tickets in DB, catalog untouched; FL3 Final list grouped by product line: per-line <section> header button with aria-expanded, "2 ideas" / "3 votes" / "2 pending" and "1 idea" / "4 votes" / "All reviewed", rows scoped to their line, header click folds (rows unmounted) and unfolds, always-visible "Mark reviewed" approves → toast Approved “<title>”, aria-pressed, header "1 pending", DB reviewed, "Mark unreviewed" reverses toast/header/DB; FL4 Label → Internal narrows to ideas with no customer on any ticket (Internal tag only on that row, chip "Internal ✕" clears), drawer source row → ticket view with labelled Customer / Reported by / Module / Created, Evaluation box "Feature request — <reason>", "Split into 2 ideas" pills titled "New idea: …" / "Merged into: …", pill opens the idea with a "Back" chevron returning to the ticket, "Back to idea" returns to the origin idea). Fixtures: FL1/FL2 run in a QA-owned org (QA-FL Clear Org, qa+fl-clear@pm-os.io, own synthetic TOTP secret, Prisma-seeded the way seed-test-db.mjs seeds RoomLens) because Clear import wipes a whole workspace and the clone's RoomLens workspace holds the developer's own import (71 ideas / 11 tickets — verified intact after the run); FL3/FL4 use QA-FL-prefixed RoomLens rows (lines, customer, 2 tickets with matchNotes, 3 ideas), delete-and-recreate in beforeAll, afterAll cleanup incl. features {} (verified: no QA-FL rows or org left). Ideas gating via the per-org {ideas:true} override as in customer-reporters/ui-facelift. Updated helpers.ts: new loginWithTotp(page, {email,password,totpSecret}) — loginAsRoomLens/loginAsRoomLensAdmin are now aliases of it (no behaviour change). Engineering's own updates on this branch (already in place, checked, not loosened): acceptConfirm helper + window.confirm → dialog in admin A1/A3, admin-delete AD1, customer-reporters CR1, ui-facelift UF2; ideas-jira-merge JM2 asserts the always-visible mark per row (no hover) and JM3's merge-column locator follows the rounded-card rename; customer-reporters CR2 suggested-chip class border-amber-400 → border-dashed. Dedup: none — no prior test covers the confirm dialog's keyboard paths, product-line grouping, the Label filter, the sidebar rail/badge, the All Customers built-in, or the drawer ticket view; the RoomLens override-set is a fixture precondition, not a re-test of admin A2. Not covered (no product finding): sidebar badge hidden-while-collapsed is asserted via the link title only. Runs: new spec green alone 4/4 (1.1m, build mode, port 3202 beside the live :3000 dev server); full suite after reseed 40 passed, 0 failed, 0 flaky, 14.3m | Omri |
| qa/release-2.0.0 (release QA 2.0.0 — IMP-09 ops spec) | 2026-09-14 | Added tests/e2e/ops-ai-failure.spec.ts (@ops IMP-09: one real upload of the golden CSV with the three Anthropic stage models pointed at a nonexistent model; first step asserts the override reached the server so the test can never pass vacuously; UI shows "Import failed: classify failed: 404 …"; batch row failed with error "classify failed: …", trace holds dedupe / jira-fetch / prepare / parse / classify, classify carries the error, split / match / customers / jira-sync / write never ran; zero tickets, zero ideas, no catalog rows beyond the built-in All Customers, /api/ideas empty after reload; ledger rows == paid AI calls — the one Gemini parse verdict — and none against the bogus model). Own org qa-ops-fail (qa+ops-fail@pm-os.io, own synthetic TOTP secret), recreated in beforeAll and kept after the run like the golden org. Harness: tests/e2e/test-env.ts gains PW_OPS_BOGUS_MODEL, the one shell variable that overrides yaml values (IDEAS_CATALOG_MODEL / IDEAS_SPLIT_MODEL / IDEAS_MATCH_MODEL — the pipeline never reads VERTEX_MODEL: the first shakedown targeted that and the import completed on claude-opus-5); global-setup.ts logs the override; package.json test:e2e adds --grep-invert @ops and test:e2e:ops runs the spec with the override; .github/workflows/e2e.yml --grep-invert "@ai\|@ops"; playwright.config.ts PW_SERVER_STDOUT=1 pipes the app's stdout into the run log (default unchanged); tests/README.md documents the two tags. Shakedown runs (headed Chrome, port 3299): run 1 the page closed 2 s into the upload (browser-side, not reproduced), runs 2–3 import completed on the real model (wrong env var, server log confirmed), run 4 green 1/1 in 15 s. Pre-existing, noted not fixed: admin-onboarding.spec.ts has four type-only errors (Prisma Json input typing on the trace helper, lines 256–269); Playwright does not typecheck, tsc is clean for every other test file. | Dana |
| qa/release-2.0.0 (release QA 2.0.0 — full run after face_lift_v2) | 2026-09-14 | No test changes (run rules: no fixes, no assertion edits, no skips, no re-runs). Full run on origin/development 42beceb (merged as 966b157 + 64d8bf4 loginWithTotp dedup) in worktree pmos-office/claude/pm-os/worktrees/release_qa_2_0_0, DB pmos_ft_release_qa after npm run test:db:setup, port 3299, real Google Chrome headed (launch log: /Applications/Google Chrome.app/…, no --headless), real Vertex eu. Result: 53 tests — 51 passed, 2 failed, 0 skipped, 22.0 m; then @ops IMP-09 as its own invocation: 1 passed, 16.6 s. Failures: (1) ai-golden.spec @ai — the live import itself completed (52 s, 25 AI calls, 11 tickets, 8 fr / 2 bug / 1 needs_details, 2 split, 021+022 merged, 9 ideas) but the spec waits for the "Imported N tickets from <file>" line that feature/face_lift_v2 removed (c0171ba, "no import summary line"); the 240 s wait expired before any step ran, so IMP-01 is recorded fail and IMP-02/03/04/05/08/10/11/13, REV-01, IMP-06 blocked — a class-A TCR candidate, not a product bug, no second attempt (not a threshold miss). DB read of the batch trace: classify 34 % and match 33 % cached (run 1: 60 %), still under the 70 % bar — the cold-org cache question for Daniel stands. (2) gating-isolation.spec GATE-02 — unchanged from run 1: /settings/google-drive renders with a Connect link and /api/google-drive/status is 200 while Drive is declared non-existent for 2.0.0 (product finding). No native window.confirm expectation is left in the suite (v2's acceptConfirm is used by admin A1/A3, AD1, CR1, UF2), so v2's dialog needs no TCR. TCR 1 (proposed, class A, awaiting Daniel): ai-golden.spec IMP-01/IMP-06 — failed on: expect(locator).toBeVisible() failed — getByText(/^Imported 11 tickets? from roomlens-golden-v2\.csv/).or(getByText(/^Import failed/)); feature: face_lift_v2 (c0171ba); old assertion caught: an upload that silently never finished or finished with another count; new assertion catches it by: wait for the Importing… overlay to end, then batch.status completed and stats.imported 11 from the DB plus the status card's "11 tickets" text (same expected values); IMP-06's "already imported" text moves to stats2.duplicates (already asserted). Results, per-case mapping, TCR and reports: pmos-office office/qa/qa-data.js run 2026-09-14-dev-42beceb and office/qa/runs/2026-09-14-dev-42beceb/. Manual rows and the step-F UI parts are recorded blocked "Daniel — QA env"; AUTH-26 stays open; AUTH-12/13 stay manual. No release sign-off: GATE-02 open, the golden run blocked on TCR 1, the IMP-10 cache bar unresolved. | Dana |
| qa/release-2.0.0 (release QA 2.0.0 — TCR 1 applied + golden re-attempt) | 2026-09-15 | TCR 1 approved by Daniel 2026-09-15, applied in aa651ba: ai-golden.spec upload wait = the Importing… overlay ending (was the "Imported N tickets from <file>" line face_lift_v2 removed in c0171ba); IMP-01's note check became the v2 status card "11 tickets · 3 parked" plus the already-asserted stats.imported; IMP-06's "already imported" text dropped in favour of the already-asserted stats2.duplicates. No other assertion changed. Re-attempt (golden spec only, real Chrome headed, port 3299, one attempt as instructed): 1 failed in 11.5 s — the QA machine's Vertex application-default credentials had expired overnight; the Gemini parse stage returned invalid_grant / "reauth related error (invalid_rapt)" and the batch failed before any Anthropic call (all-or-nothing held: 0 tickets, 0 ideas). Environment, not product or test; gcloud confirms "Reauthentication failed… run gcloud auth application-default login". Recorded as run 2026-09-15-dev-42beceb-tcr1 in pmos-office office/qa/qa-data.js (golden ids blocked with the verbatim error; every other case carries over from 2026-09-14-dev-42beceb), report under office/qa/runs/2026-09-15-dev-42beceb-tcr1/. No second attempt taken. Next: Daniel reauthenticates ADC, then one golden attempt; after that the TCR rule still requires a full-suite run before sign-off. | Dana |
