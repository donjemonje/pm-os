# QA log

Append-only, kept by Dana (`qa-manager`). Every feature branch adds its row
BEFORE review — no row, no review. Newest entries at the bottom.

| Feature branch | Date | Tests added/updated | QA'd by |
| --- | --- | --- | --- |
| feature/tests-setup | 2026-08-26 | Added tests/e2e/auth.spec.ts (login renders, RoomLens login + app shell, docs/releases nav) | Omri |
| feature/tests-setup | 2026-08-26 | Added tests/e2e/all-pages.spec.ts (all-pages sweep, logged-in redirects, CRM auth-realm isolation, ideas 404s) + expectAppPageRenders helper; removed auth.spec.ts docs/releases nav test (subsumed by the sweep) | Dana & Omri |
| feature/tests-setup | 2026-08-26 | Infra, no test changes: env loading moved into playwright.config.ts (bare npx/--ui runs match npm script); env guard in global-setup.ts + test-env.ts (login enabled, pmos_test only, SESSION_SECRET, port/app-URL match, IDEAS_ENABLED off). Guard failure path verified by removing yaml | Dana & Omri |
| feature/tests-setup | 2026-08-26 | Prod-testing removal (Daniel's order), no assertion changes: prod-smoke project + scripts + smoke tags stripped; seed renamed scripts/seed-test-db.mjs, env overrides removed, hardcoded synthetic creds, refuses NODE_ENV=production; env guard unchanged. Repo grep clean for prod terms | Dana & Omri |
| feature/npm-audit-fixes | 2026-08-26 | None added — dependency/security chore, no new product behavior (audit bumps; Next 15.5.23→16.3.2; middleware.ts→proxy.ts; lint script removed; jsx react-jsx). Existing suite already covers the risky surface: redirect/realm specs exercise the renamed proxy, all-pages sweep covers rendering. Full suite re-run green on Next 16 (6/6, 13.0s); clean build 48/48 pages. Accepted residual: deepmerge-ts <8 via @prisma/config pin (dev CLI only) | Dana |
| feature/admin-restriction | 2026-08-27 | Added tests/e2e/admin.spec.ts (A1 promote/demote + self-role/self-deactivate guardrail refusals in UI; A2 per-org ideas override gates a second user's context On→reachable / Default→404; A3 deactivate revokes live session + blocks login, reactivate restores in the same context — tightened after the stale-cookie 500/lockout bug Omri found, fixed in fec4188 via the /api/auth/session-expired exit ramp). Updated: all-pages.spec CRM auth-realm test replaced by USER-404-on-/admin (CRM became role-gated /admin — subsumption); helpers.ts adds QA_ADMIN + loginAsRoomLensAdmin; two-factor-helpers.ts per-secret code-window tracker (backward compatible); seed-test-db.mjs seeds qa+roomlens-admin (PMOS_ADMIN, own synthetic TOTP secret) and resets roles/deactivation/org overrides; admin.spec resets fixtures in beforeAll+afterAll. Not e2e: last-active-admin guard stays unit-level (actor is always the last admin, self-rule fires first — unreachable end-to-end). Dedup ruling: A2's baseline /ideas 404 kept alongside the all-pages ideas test (load-bearing precondition, not a duplicate flow). Suite green by Dana: 13 passed, 1 env-gated skip | Dana & Omri |
| feature/2fa | 2026-08-26 | Added tests/e2e/two-factor.spec.ts (T1 challenge layout, T2 previous-window rejected, T3+T4 valid code + no replay, T5 unverified session 401s, T6 forced enrollment) + two-factor-helpers.ts; added tests/e2e/register.spec.ts (signup→enrollment, env-gated skip while ALLOW_SIGNUP=false mirrors prod). Updated: loginAsRoomLens completes the mandatory TOTP challenge; seed-test-db.mjs enrolls qa+roomlens with the fixed test secret (app's own encryptTotpSecret, --experimental-strip-types), resets totp state + sessions each run; env guard checks TOTP_ENC_KEY (set + 64 hex); Playwright timeout 30s→60s (TOTP window waits by design). Removed: tests/e2e/seed-2fa.ts (targeted the DEV database — rule violation); auth.spec.ts app-shell login test (subsumed by T3 + all-pages sweep). Suite green twice by Dana (10 passed, 1 env-gated skip; second run with no re-seed) | Dana & Omri |
| feature/admin-restriction (rework) | 2026-08-27 | Second sign-off after post-review changes. Updated admin.spec A1 (rewritten for script-only roles, Daniel's security call: role PATCH 400s with a script pointer, bundled role+deactivate payload rejected whole with no partial application, UI renders zero role affordances — guarded against vacuous passes by asserting the Deactivate button IS in the same actions column; self-deactivation refusal kept, message asserted verbatim). Updated admin.spec A2 (moved to /admin/enablements after the Configurations rename; row-scoped selectors per flag; ideas full override round-trip kept; extended with a chat Off→404→Default round-trip covering the reversed env polarity — page and /api/chat/sessions both 404 — and restoring default before all-pages runs). Updated all-pages.spec (/admin/configurations → /admin/enablements in the USER-404 loop) and env guard (DOCS_ENABLED/CHAT_ENABLED must not resolve false-like — both on when unset). Dedup ruling: no separate docs toggle test — docs rides the identical resolver/gate path (featureEnabledForCurrentUser + orgFeatureDisabledResponse + registry-driven Enablements UI, verified by grep across the 7 docs handlers and layout), override-wins is proven by ideas, polarity by chat, docs default-on rendering by the all-pages sweep. Accepted product gap (documented in 63647be): Releases attach-document UI still renders with docs off; its API 404s. Suite green by Dana: 13 passed, 1 env-gated skip, 3.5m | Dana & Omri |
| feature/customer-reporters | 2026-08-31 | Added tests/e2e/customer-reporters.spec.ts (CR1 Settings → Ideas Customers panel add / case-insensitive duplicate rejection / delete, with reload-persistence checks — first coverage of the shared KINDS CRUD panel; CR2 drawer derives reporters from ticket requesters and customers from affectedCustomers, cataloged name renders teal, off-catalog name renders as amber suggested chip, ✓ approve adds it to the catalog + flips the chip teal live + logs approve_customer; CR3 ✕ dismiss removes the chip, "dismissed" toggle shows the strikethrough entry, restore brings the suggestion back untouched-by-catalog, dismiss_customer + undismiss_customer both on the ledger; CR4 list rows carry chips with the ⚑ suggested flag, Customer FilterRow narrows to matching ideas and clears via the selected chip). Fixtures seeded via Prisma in beforeAll exactly as the v5 extract stage would persist them (no LLM calls), QA-CR-prefixed, delete-and-recreate + afterAll cleanup so reruns need no re-seed. Ruling: ideas stays OFF in env + seed; the spec sets the per-org {ideas: true} override in beforeAll and clears it in afterAll (admin A2 pattern) — enabling it in the seed would break the all-pages ideas-404 sweep. Residual narrower than feared: admin.spec runs first alphabetically and resets features:{} in beforeAll, so a crash-leaked override self-heals on any full-suite rerun; only a filtered rerun skipping admin.spec can hit it. Dedup: none — no existing test renders /ideas or /settings/ideas content; the override-set is a fixture precondition, not a duplicate of admin A2's override flow. Suite green by Dana: 17 passed, 1 env-gated skip, 5.7m | Dana & Omri |
| feature/ideas-jira-merge | 2026-08-31 | Added tests/e2e/ideas-jira-merge.spec.ts (JM1 Final page hides unchanged ideas by default, Unchanged chip reveals them, Import Status chips are single-select — click replaces, re-click clears; JM2 approval exemption: unchanged rows never grow the Mark-reviewed affordance, drawer has no Approve button, and /api/ideas/mutate refuses decision "reviewed" for an approval-exempt idea — no-op verified in returned state AND DB, with a positive control flipping the New idea on the same endpoint then restoring it; JM3 Merge view: unchanged ideas + Jira sources backing only unchanged ideas hide under Merge/Single and show under Unchanged, edit mode shows the full source pool; JM4 manual reassign: attaching a ticket flips an unchanged Jira idea to Updated with the zendesk-vote delta (+1 badge, newVotes 0→1, existingVotes untouched), detaching rolls status and delta back). LLM-free: the Vertex match stage stays an accepted e2e gap; fixtures (JiraIdeaSnapshot + Jira-origin ideas by jiraKey, ZendeskTicketRaw + zendesk IdeaSources, QA-JM prefixes) are Prisma-seeded exactly as the import would persist them, delete-and-recreate beforeAll + afterAll cleanup, JM2/JM4 reverse their own writes. Ideas gating: per-org {ideas: true} override set in beforeAll / cleared in afterAll (customer-reporters pattern; env + seed stay ideas-off for the all-pages 404 sweep — spec order and admin.spec's reset keep the residual self-healing). Dedup: none — no prior test touches Import Status chips, the merge board, approval exemption, or reassign; the override-set is a fixture precondition, not a re-test of admin A2. Updated customer-reporters.spec CR1 (found in Dana's verification run): first-hit customers-API POST can outlive the default 5s expect timeout under local `next dev` on-demand compile — snapshot showed the form correctly disabled mid-flight, product fine; the post-Add visibility assert now has a 15s timeout (timing-by-design headroom, same class as the TOTP waits; no assertion loosened). Suite green by Dana after the fix: 21 passed, 1 env-gated skip, 7.6m (first run had the CR1 cold-compile failure, reported as found) | Dana & Omri |
| feature/google-sso | 2026-09-01 | Added tests/e2e/google-sso.spec.ts (G1 googleSso system-flag round-trip on the new /admin/enablements System card — Off (default)→On→Off→Default, each state observed on /api/auth/oauth/providers AND the authorize redirect: Google consent URL when on, /login?error=google_sso_disabled when off; fake test creds make the provider "configured", no call ever reaches Google; G2 forgot-password enumeration safety — identical 200 {ok:true} for real and nonexistent email, token row exists only for the real user, unused, future expiry; G3 full reset path — DB-inserted token hashed exactly as requestPasswordReset stores it → /reset-password UI sets the new password → same-token replay 400s without clobbering it → new password logs in through the mandatory TOTP challenge; G4 expired token gets the same generic 400 and the row stays unused). State discipline: beforeAll+afterAll clear the googleSso override, purge PasswordResetToken, and restore QA_USER's seeded passwordHash via a fresh app-format re-hash — no leak into other specs, rerunnable without reseed (verified). Updated global-setup.ts env guard (DISABLE_GOOGLE_LOGIN must resolve true-like; fake GOOGLE_CLIENT_ID/SECRET required) with matching creds in test-apphosting(.example).yaml + CI. Updated auth.spec (Dana): the logged-out render test now clicks the /login "Forgot password?" link and asserts the /forgot-password form — the link is the reset flow's only entry point and G2–G4 bypass it via API/direct URL, so it could vanish silently otherwise; also proves the proxy public-path entry. Updated all-pages.spec route inventory (comment only): /forgot-password + /reset-password documented as public, covered in auth.spec/G3, deliberately absent from the logged-in redirect loop (proxy does not bounce a signed-in user off them — same class as /login/2fa). Dedup: none — G1 is first coverage of the SystemFlag mechanism, distinct from admin A2's per-org override per the 2026-08-27 registry ruling; G3's TOTP login is a means of proving the password change, not a re-test of two-factor T3. Accepted gap, flagged in the spec header: sticky login type (email_uses_password in signInWithOAuth) needs a mocked Google token endpoint — review-covered only. Suite green by Dana: 25 passed, 1 env-gated skip, 7.9m; auth+google-sso rerun green without reseed | Dana & Omri |
| feature/ci-test-fixes | 2026-09-02 | None added — suite-repair chore for the two CI failures on development since the feature/support_import_to_jira merge (85be916), both test-only defects, product behaviour correct. Updated admin.spec A2: the merge added an "Ideas undo" row to /admin/enablements, so the row-scoped `li` locator with hasText "Ideas" matched two rows (strict-mode violation); now `.filter({ hasNotText: "Ideas undo" })` — verified against the FLAGS registry that no other label/description contains "ideas" (hasText is case-insensitive substring; a regex `/^Ideas\s+(On|Off)\b/` was tried first and fails because the post-click row text is "Ideas OnIdeas pipeline…" with no word boundary — do not retry). Updated ideas-jira-push.spec PJ2: the invalid-JSON case sent a string body under a JSON content-type, which Playwright 1.62 JSON.stringify-es when unparsable, so the route received a valid JSON string and the assertion failed on "mode must be preview or execute"; now `data: Buffer.from("{not json")` reaches the route's JSON catch untouched — PJ2's "Invalid JSON" assertion is exercised for real for the first time. No assertion loosened in either fix. Process finding for the record: feature/support_import_to_jira merged with no QA-LOG row and with a red suite (ideas-jira-push.spec PJ1–PJ3 entered the suite via that merge without a sign-off row). Suite: full run green by engineering (28 passed, 1 env-gated skip, 8.9m); admin + ideas-jira-push re-run green by Dana (6 passed, 2.7m) | Dana |
